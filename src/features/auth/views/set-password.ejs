<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body class="auth-page">
    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-header">
                <h1>
                    <%= title %>
                </h1>
                <p class="text-muted">Chào mừng <%= user.hoTen %>!</p>
            </div>

            <% if (locals.error || query.error) { %>
                <div class="alert alert-danger" role="alert">
                    <%= locals.error || query.error %>
                </div>
                <% } %>

                    <form method="POST" action="/auth/register/<%= token %>">
                        <div class="form-group">
                            <label for="password">Mật khẩu mới *</label>
                            <input type="password" class="form-control" id="password" name="password" required
                                minlength="6" placeholder="Tối thiểu 6 ký tự">
                        </div>

                        <div class="form-group">
                            <label for="confirmPassword">Xác nhận mật khẩu *</label>
                            <input type="password" class="form-control" id="confirmPassword" name="confirmPassword"
                                required minlength="6" placeholder="Nhập lại mật khẩu">
                        </div>

                        <div class="form-group">
                            <p class="text-muted small">
                                <strong>Lưu ý:</strong> Mật khẩu phải có ít nhất 6 ký tự.
                            </p>
                        </div>

                        <button type="submit" class="btn btn-primary btn-block">
                            Thiết lập mật khẩu
                        </button>
                    </form>

                    <div class="auth-footer mt-3">
                        <p class="text-muted small">
                            Sau khi thiết lập mật khẩu, bạn sẽ được tự động đăng nhập vào hệ thống.
                        </p>
                    </div>
        </div>
    </div>

    <script>
        // Client-side validation for password match
        document.querySelector('form').addEventListener('submit', function (e) {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirmPassword').value;

            if (password !== confirmPassword) {
                e.preventDefault();
                alert('Mật khẩu xác nhận không khớp!');
                return false;
            }
        });
    </script>
</body>

</html>