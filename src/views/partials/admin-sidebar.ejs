<div class="col-md-3 col-lg-2 sidebar">
    <div class="d-flex align-items-center p-3">
        <i class="fas fa-bars me-2 text-white"></i>
        <span class="text-white fw-bold">BẢNG CHỌN ADMIN</span>
    </div>

    <nav class="nav flex-column mt-3">
        <!-- TRANG CHỦ ADMIN -->
        <a class="nav-link <%= currentPage === 'admin-home' || currentPage === 'users' ? 'active' : '' %>" 
           href="/admin">
            <i class="fas fa-home me-2"></i>
            Trang chủ Admin
        </a>

        <!-- QUẢN LÝ NGƯỜI DÙNG -->
        <h6 class="sidebar-heading text-muted mt-3 mb-1 px-3">Quản lý hệ thống</h6>
        
        <a class="nav-link <%= currentPage === 'users' ? 'active' : '' %>" 
           href="/admin">
            <i class="fas fa-users me-2"></i>
            Quản lý người dùng
        </a>
        
        <a class="nav-link <%= currentPage === 'users-create' ? 'active' : '' %>" 
           href="/admin/create">
            <i class="fas fa-user-plus me-2"></i>
            Tạo người dùng
        </a>

        <!-- TODO: Thêm các chức năng admin khác -->
    </nav>
</div>

<style>
    .sidebar-heading {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.05rem;
        font-weight: 700;
        color: #94a3b8 !important;
    }
    .sidebar {
        background-color: #1e3a8a;
        min-height: 100vh;
        padding: 0;
    }
    .sidebar .nav-link {
        color: white;
        padding: 10px 15px;
        transitio