<!-- Department Head Header -->
<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: var(--primary-blue);">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/teacher">
            <i class="fas fa-graduation-cap me-2"></i>
            HỆ THỐNG QUẢN LÝ THIẾT BỊ TRƯỜNG THCS
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link <%= (currentPage === 'teacher-home' || currentPage === 'borrow') ? 'active' : '' %>" href="/teacher/borrow/teacher-home">
                        <i class="fas fa-home me-1"></i>Trang chủ
                    </a>
                </li>
            </ul>
            
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user-circle me-2 fs-5"></i>
                        <span><%= typeof user !== 'undefined' && user.hoTen ? user.hoTen : 'Tổ trưởng chuyên môn' %></span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="/profile">
                            <i class="fas fa-user me-2"></i>Thông tin cá nhân
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item text-danger" href="/auth/logout">
                            <i class="fas fa-sign-out-alt me-2"></i>Đăng xuất
                        </a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

