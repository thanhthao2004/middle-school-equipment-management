<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || "EduEquip" %></title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body { font-family: "Segoe UI", Arial, sans-serif; background: #f5f6fa; margin: 0; }
    .top-bar { background: #0d47a1; color: white; padding: 10px 20px; display: flex; justify-content: space-between; align-items: center; }
    .main-layout { display: flex; min-height: calc(100vh - 50px); }
    .sidebar { width: 230px; background: linear-gradient(180deg, #1565c0, #0d47a1); color: white; display: flex; flex-direction: column; padding: 15px; }
    .sidebar-item { display: flex; align-items: center; gap: 10px; padding: 10px; border-radius: 6px; cursor: pointer; transition: background 0.3s; }
    .sidebar-item:hover, .sidebar-item.active { background: rgba(255,255,255,0.2); }
    .content-area { flex: 1; padding: 25px; }
    h1 { font-size: 22px; margin-bottom: 20px; color: #212121; }
    table { width: 100%; border-collapse: collapse; background: white; border-radius: 8px; overflow: hidden; }
    th, td { padding: 10px; border-bottom: 1px solid #e0e0e0; text-align: center; }
    thead { background: #f0f0f0; font-weight: 600; }
    tbody tr:nth-child(even) { background: #e3f2fd; }
    tbody tr:hover { background: #bbdefb; }
    .btn { border: none; border-radius: 5px; padding: 6px 12px; cursor: pointer; font-size: 14px; transition: 0.3s; }
    .btn-primary { background: #1565c0; color: white; }
    .btn-primary:hover { background: #0d47a1; }
    .status { display: inline-block; padding: 4px 10px; border-radius: 12px; font-weight: 600; }
    .status.approved { background: #c8e6c9; color: #2e7d32; }
    .status.pending { background: #fff59d; color: #f57c00; }
    .status.warning { background: #ffe0b2; color: #e65100; }
  </style>
</head>
<body>
  <div class="top-bar">
    <div class="logo">🎓 EduEquip</div>
    <div class="user">👤 QLTB</div>
  </div>

  <div class="main-layout">
    <div class="sidebar">
      <div class="sidebar-item"><i class="fas fa-chart-bar"></i> Bảng chọn</div>
      <div class="sidebar-item"><i class="fas fa-file-contract"></i> Biên bản nghiệm thu</div>
      <div class="sidebar-item"><i class="fas fa-chart-line"></i> Báo cáo định kỳ</div>
      <div class="sidebar-item"><i class="fas fa-laptop"></i> Mượn thiết bị</div>
      <div class="sidebar-item"><i class="fas fa-chart-pie"></i> Báo cáo thống kê thiết bị</div>
      <div class="sidebar-item"><i class="fas fa-graduation-cap"></i> Kế hoạch đào tạo</div>
      <div class="sidebar-item"><i class="fas fa-shopping-cart"></i> Kế hoạch mua sắm</div>
    </div>

    <div class="content-area">
      <%- body %>
    </div>
  </div>
</body>
</html>
