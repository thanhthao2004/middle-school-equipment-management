<%
// Xác định header type dựa trên userRole
const headerType = typeof userRole !== 'undefined' ? userRole : 'giao_vien';
const headerTypes = {
    'admin': 'admin-header',
    'ql_thiet_bi': 'qltb-header',
    'giao_vien': 'teacher-header',
    'to_truong': 'department-head-header',
    'hieu_truong': 'principal-header'
};
const headerFile = headerTypes[headerType] || 'teacher-header';
%>

<!-- Include header theo actor -->
<%- include('./headers/' + headerFile, {
    user: typeof user !== 'undefined' ? user : {},
    currentPage: typeof currentPage !== 'undefined' ? currentPage : ''
}) %>
